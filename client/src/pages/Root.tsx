import { useState } from "react";
import RestAPI from "./RestAPI";

function Root() {
    // storing instances of the same element
  const [tabs, setTabs] = useState([]);
  return (
    <div>

      
    </div>
  );
}

export default Root;
